{{- if .Values.spec.eventManagerGateway.enabled }}
apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: cp4waiops-operator-group
  namespace: {{.Values.spec.eventManagerGateway.namespace}}
  annotations:
    argocd.argoproj.io/sync-wave: "110"
spec:
  targetNamespaces:
    - {{.Values.spec.eventManagerGateway.namespace}}
{{- end }}