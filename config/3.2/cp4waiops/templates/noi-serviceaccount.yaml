{{- if .Values.spec.eventManagerGateway.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: noi-service-account
  namespace: {{.Values.spec.eventManagerGateway.namespace}}
imagePullSecrets:
- name: ibm-entitlement-key
{{- end }}